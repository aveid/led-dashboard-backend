"""ports.py — прикладные порты раздела «Пользователи».

Хеширование пароля — инфраструктурная деталь (bcrypt/passlib), но использовать
его должен прикладной сценарий (create/update). Чтобы use cases остались чистыми
и тестируемыми, зависимость выражена узким портом-протоколом; конкретная
реализация (обёртка над config.security.hash_password) подставляется снаружи.
Второй хешер НЕ заводим — переиспользуем существующий из auth (инструкция §2.5).
"""

from __future__ import annotations

from typing import Protocol


class PasswordHasher(Protocol):
    """Порт хеширования паролей: домен/приложение знают только про hash()."""

    def hash(self, plain_password: str) -> str:
        """Вернуть безопасный хеш пароля для хранения."""
        ...
