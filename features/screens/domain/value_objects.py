"""value_objects.py — объекты-значения фичи «экраны».

За что отвечает модуль:
    Определяет неизменяемые составные значения, специфичные для экранов.

История модуля:
    • условия аренды раньше были здесь как value object, но по решению Q3 (нужна
      история аренды) договор стал полноценной СУЩНОСТЬЮ с идентификатором и
      переехал в entities.py (RentalContract);
    • город раньше был строковым полем адреса, но был промотирован в отдельную
      сущность-справочник City (features/cities); экран ссылается на город по
      city_id (см. entities.Screen);
    • район (district) и улица (street) удалены из модели экрана (FR-3.3): адрес
      на карточке больше не редактируется текстом, локация задаётся точкой на
      карте (координаты — GeoPoint из core.domain.value_objects). Вместе с ними
      исчез и value object Address — собственных объектов-значений у фичи сейчас
      нет.

Общие value objects (Money, GeoPoint) живут в core.domain.value_objects.
"""

from __future__ import annotations
