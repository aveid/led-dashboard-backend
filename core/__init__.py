"""core — общее ядро (shared kernel) проекта.

Здесь лежат переиспользуемые между всеми фичами строительные блоки:
    • domain      — базовая сущность, общие value objects (Money, GeoPoint), доменные ошибки;
    • application — базовый класс use case;
    • api         — общие для DRF вещи (обработчик ошибок, пагинация).

Правило: код в core не должен зависеть от конкретных фич (apps/*).
Зависимость идёт в одну сторону: фичи используют core, но не наоборот.
"""
